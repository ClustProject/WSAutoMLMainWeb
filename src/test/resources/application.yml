cloud:
  aws:
    s3:
      bucketName: testBuckName
    region:
      static: ap-northeast-2
    stack:
      auto: false
    credentials:
      instanceProfile: true
      accessKey: test
      secretKey: test

spring:
  security:
    oauth2:
      client:
        registration:
          google:
            scope: profile, email
            client-id: test
            client-secret: test
  jpa:
    hibernate:
      ddl-auto: create-drop
