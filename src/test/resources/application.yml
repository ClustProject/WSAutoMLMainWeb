spring:
  security:
    oauth2:
      client:
        registration:
          google:
            scope: profile, email
            client-id: test
            client-secret: test
  jpa:
    hibernate:
      ddl-auto: create-drop
